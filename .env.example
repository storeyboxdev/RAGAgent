# Supabase (populated by `npx supabase start`)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# LMStudio (connects via WebSocket - no URL/API key needed)
# LLM model identifier (optional - falls back to whatever is loaded in LMStudio)
LLM_MODEL=qwen2.5-7b-instruct

# Embedding model identifier
EMBEDDING_MODEL=nomic-embed-text-v1.5
EMBEDDING_DIMENSIONS=768

# Laminar
LMNR_PROJECT_API_KEY=

# Docling (document parsing for PDF, DOCX, HTML)
DOCLING_SERVE_URL=http://localhost:5001

# Server
PORT=3001
